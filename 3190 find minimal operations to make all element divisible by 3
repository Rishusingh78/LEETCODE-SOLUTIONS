class Solution {
public:
    int minimumOperations(vector<int>& nums) {
        int operations = 0;

        for (int num : nums) {
            int r = num % 3;
            operations += min(r, 3 - r);
        }

        return operations;
    }
};
